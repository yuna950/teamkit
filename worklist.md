# TeamKit 작업 기록

다른 컴퓨터에서 이어서 확인할 수 있도록 기획부터 지금까지의 결정 사항과 진행 상황을 전부 정리한 문서.

## 1. 프로젝트 개요

대학생 팀 프로젝트를 위한 협업용 자료 및 할 일 관리 웹앱.
프론트 디자인(와이어프레임 기반 UI)을 전부 작업한 뒤 백엔드(Supabase) 작업에 들어가는 순서로 진행.

- **기술 스택**: React + Vite + TypeScript + Tailwind CSS + Supabase
- **코드 위치**: `teamkit/app` (Vite 프로젝트 루트)
- **레퍼런스**: `teamkit/레퍼런스 이미지/` (스타일가이드, 와이어프레임 9종)

## 2. 확정된 스펙

### 인증
- 이메일/비밀번호(Supabase Auth), 소셜 로그인 없음
- **미결(보류)**: 비밀번호 재설정 여부, 이메일 인증 여부 → 백엔드 연동 단계에서 결정

### 자료
- 필드: 제목 / 단계 / 메모 / 첨부(파일·이미지·영상·링크)
- 단계 필터: 기획 → 자료조사 → 발표준비
- 작성자 필터
- 고정: 첨부파일 단위, 프로젝트 전체 공유, 권한 무관 누구나 고정 해제 가능
- 자료 추가 모달 필수값: **제목 + 첨부자료(파일 또는 링크) 최소 1개**, 메모는 선택
- 리스트 화면에서 메모는 제목과 첨부파일 사이에 연한 색 텍스트로 노출

### 체크리스트
- 공통(담당자 없이 프로젝트 전원 공유) / 개인 구분
- 필드: 할 일 / 메모 / 마감일 (우선순위 없음)
- 체크리스트 추가 모달 필수값: **할 일만 필수**, 메모·마감일은 선택. 마감일은 날짜만 또는 날짜+시간(선택) 지정 가능
- 마감임박 = 3일 이내 + 연체 (대시보드 위젯 노출 기준)
- 완료 시 취소선 + 키컬러(브랜드 퍼플) 체크박스 + 리스트 하단으로 이동
- 모달 팝업으로 추가

### 회의록
- 필드: 제목 / 날짜 / 내용 (필수), 참석자(팀원 다중선택, 칩 토글 방식)
- 서식 툴바: 굵게 + 목록 2개 버튼만, **textarea 상단 고정 노출**(와이어프레임엔 미반영 상태였고, 기능 추가하는 방향으로 확정)
- 저장 버튼 상단 고정
- 전원 동등 CRUD (관리자/팀원 구분 없음)
- 리스트 화면(카드형)과 작성/수정 화면(풀페이지 에디터)은 별도 화면

### 권한 / 탈퇴
- 관리자 전용: 팀원 강퇴, 프로젝트 정보 수정, 프로젝트 삭제, 팀원 역할 변경(관리자 위임)
- 전원 허용: 초대 링크 발급/재발급, 자료·체크리스트·회의록 CRUD
- 위임 필수 범위: 프로젝트 나가기 + 회원 탈퇴 **둘 다** (관리자면 위임 후 가능), 위임 시 토스트 알림
- 설정 탭은 전체 팀원에게 노출되고 관리자 전용 버튼만 비활성화 처리

### 초대
- 7일 만료, 재발급 시 기존 링크 무효화, 만료 시 안내 화면
- 발급/재발급 권한: 전체 팀원 허용

### 마이대시보드 / 프로젝트
- 비로그인 시 마케팅 랜딩페이지
- 프로젝트 필드: 마감일, 설명
- 프로젝트 고정 기능 있음 (고정된 프로젝트 / 나의 프로젝트 섹션 분리)
- 활동 피드는 이번 범위 제외

### UI 공통 규칙
- 모든 "추가" 모달의 저장 버튼명은 "저장"으로 통일
- 반응형(모바일 포함)
- **저장 버튼 활성화 규칙**: 모든 "저장" 버튼(모달 내부 + 회의록 작성 화면의 상단 고정 저장 버튼 포함)은 기본 비활성 상태(soft/흐린 스타일)로 시작하고, 제목 등 필수 입력값이 채워지면 활성(solid/진한 스타일)로 전환됨
- **타이틀 크기 위계**: 페이지 타이틀(h1, 예: 추후 만들 프로젝트 상세 탭 제목 "대시보드"/"자료" 등) = 30px(`text-3xl`) bold / 섹션 타이틀(h2, 예: "고정된 프로젝트") = 24px(`text-2xl`) bold / 모달 제목 = 20px(`text-xl`) bold
- **파괴적 액션 확인**: 삭제 등 되돌릴 수 없는 액션은 브라우저 기본 `alert`/`confirm` 대신 자체 제작 `ConfirmDialog` 컴포넌트 사용 (제목+설명+취소/확인 버튼, 확인 버튼은 `Button variant="danger"`)

### 사이드바 탭 순서 (확정)
`대시보드 → 자료 → 체크리스트 → 회의록 → 설정`

## 3. IA (정보구조)

```
로그인/회원가입 → HOME
HOME ─ 마이대시보드 → 프로젝트 목록 → 프로젝트 상세
    └ 마이페이지 → 회원정보 수정

초대링크 ─(로그인 O)→ 프로젝트 상세
        └(비로그인)→ 로그인/회원가입 → 프로젝트 상세

프로젝트 상세 ─ 대시보드 (마감임박 할 일 / 고정 자료)
           ├ 자료 리스트
           ├ 체크리스트 (공통 / 개인)
           ├ 회의록
           └ 설정
```

## 4. 스타일가이드 (레퍼런스 이미지 반영)

- **컬러**: 브랜드 퍼플 3단계(#F0EBFF / #7654D6 / #6544C4), 뉴트럴 5단계(#FFFFFF~#191B1F), 시맨틱(그린 #22A06B, 오렌지 #E58A00 / #FAE6C8, 레드 #D64545 / #FDD3D3)
- **D-day 배지**: D-3 이하=레드, 중간 구간=오렌지, 그 외=그레이 (정확한 색상 전환 임계값은 추후 세부 조정 예정)
- **버튼**: "저장" 연보라(soft)형과 진보라 솔리드형 두 변형은 서로 다른 버튼이 아니라 **같은 저장 버튼의 비활성/활성 상태**였음(확인 완료). 필수값 미입력 시 soft 톤(비활성) → 필수값 입력 완료 시 solid 톤(활성)으로 전환
- **솔리드 버튼 hover/active**: 더 어두운 톤으로 (brightness 필터 사용, hover 90%/active 75%)
- **모든 버튼**: hover 시 `cursor: pointer` (전역 CSS로 처리)

## 5. 라우트 구조 (확정)

```
[공개] (비로그인 전용, 로그인 시 /projects로 리다이렉트)
/                          → 랜딩페이지
/login                     → 로그인
/signup                    → 회원가입
/invite/:token             → 초대링크 처리
/invite/expired            → 초대 만료 안내

[보호] (로그인 필요)
/projects                  → 마이대시보드 (프로젝트 목록)
/my-page                   → 마이페이지

/projects/:projectId                  → 프로젝트 상세 레이아웃 (5탭 사이드바)
  ├─ (index → /dashboard 리다이렉트)
  ├─ /dashboard
  ├─ /materials              (?stage=, ?author= 쿼리 파라미터로 필터)
  ├─ /checklist
  ├─ /minutes
  ├─ /minutes/new
  ├─ /minutes/:minuteId
  └─ /settings

*                          → 404
```

## 6. 전체 작업 로드맵 & 진행 상황

### Phase 1 — 프로젝트 뼈대 ✅ 완료
- [x] `teamkit/app`에 Vite(React+TS) 스캐폴딩
- [x] Tailwind CSS v4 설정 (`@tailwindcss/vite`) + 스타일가이드 색상 토큰 등록 (`src/index.css` `@theme`)
- [x] react-router-dom 설치, 확정된 라우트 구조 그대로 뼈대 세팅 (`src/routes/AppRoutes.tsx`, 3개 레이아웃, 모든 탭 placeholder 페이지)
- [x] 폴더 구조(`layouts/pages/routes/components`), `@/*` 경로 별칭, oxlint 세팅
- 빌드(`npm run build`) / 린트(`npm run lint`) 통과 확인

### Phase 2 — 공통 디자인 시스템 컴포넌트 ✅ 완료
`src/components/ui/`에 아래 컴포넌트 생성 완료 (barrel export: `src/components/ui/index.ts`)
- [x] Button (solid/soft/outline), IconButton
- [x] Input, Textarea
- [x] Checkbox, Radio
- [x] Badge + D-day 판정 로직(`getDdayTone`, `DdayBadge`) — 3일 이하 danger / 4~7일 warning / 그 외 neutral
- [x] Avatar, Chip, Tabs
- [x] Dropdown(+ DropdownItem) — 정렬 선택·프로젝트 전환·케밥 메뉴에 재사용
- [x] DatePicker — 달력 그리드 + "시간 추가" 토글(시간 미지정 가능) + 휠(드럼) 스타일 시간 선택(`TimeWheel`/`WheelColumn`, CSS scroll-snap 기반, 외부 라이브러리 없음). 값 형태는 `{ date: Date; time: string | null }`. 오늘=연한 강조, 선택된 날짜=브랜드퍼플 솔리드로 진하게 강조
- [x] Switch — "시간 추가" 토글 등에 사용하는 공통 스위치
- [x] Modal — portal 기반, ESC/배경 클릭으로 닫힘
- [x] Toast — `ToastProvider` + `useToast()` 훅, `App.tsx` 최상단에 Provider 연결
- [x] Card, FileTypeIcon(확장자별 아이콘 매핑)
- [x] ConfirmDialog — 삭제 등 되돌릴 수 없는 액션 확인용(제목/설명/취소·확인 버튼), Button에 `danger` variant 추가
- [x] `/dev/ui-kit` 페이지에 전 컴포넌트 데모 모아둠 (아래 "개발 중 화면 확인" 참고)
- [ ] 인증 前/後 공통 레이아웃 다듬기 → Phase 3에서 실제 페이지 작업 시 진행

### Phase 3 — 핵심 화면 (더미 데이터 기반)
랜딩페이지는 기획 전이라 보류, 순서를 로그인/회원가입 우선으로 재조정함(아래 "개발 중 화면 확인" 참고)
- [x] **로그인/회원가입 + 임시(mock) 인증** — `src/auth/AuthContext.tsx`. localStorage 기반, 테스트 계정 `test@teamkit.com` / `test1234` 시드 등록. 로그인 페이지에 "테스트 계정으로 로그인" 원클릭 버튼 있음. 회원가입 시 이름 필드 추가(참석자·작성자 표시용, 스펙에 명시 안 됐지만 필요해서 추가함). `/projects`,`/my-page`,`/projects/:id/*`는 `RequireAuth`로 보호, `/login`·`/signup`은 `RedirectIfAuthed`로 로그인 시 접근 차단. `/`는 랜딩 나오기 전까지 임시로 `/login`으로 리다이렉트
- [x] **마이대시보드 `/projects`** — 더미 프로젝트 데이터(5개), 고정된 프로젝트/나의 프로젝트 섹션(고정하면 나의 프로젝트에서 빠지고 상단 섹션으로 이동), 정렬 드롭다운(마감일순/이름순, 실제 동작), 핀 토글(클릭 시 고정↔해제, 채움/아웃라인으로 상태 구분), "+ 새 프로젝트 생성하기" 모달(이름 필수, 설명·마감일 선택, 필수값 채워야 저장 버튼 활성화). 케밥(⋮) 메뉴는 **초대 링크 복사 / 수정 / 삭제** (고정은 옆 핀 아이콘에 이미 있어서 메뉴에서 제외, 재발급처럼 기존 링크를 무효화하는 위험한 동작은 여기 안 넣고 프로젝트 설정 탭에만 두기로 함)
- [x] **프로젝트 상세 공통 레이아웃 다듬기** (`ProjectLayout`) — 프로젝트 데이터 공유화(`ProjectsContext`), 상단 프로젝트 전환 드롭다운, 사이드바 하단 로그인 사용자 이름 표시, 존재하지 않는 프로젝트 ID 접근 시 `/projects`로 리다이렉트
- [x] **대시보드 탭** (`ProjectDashboardPage`) — 좌측 체크리스트 요약(공통/개인 카드, 체크박스로 완료 토글, 완료 시 취소선+하단 이동), 우측 고정된 자료 목록(파일타입 아이콘+작성자+용량). 이 참에 **자료**(`src/materials/MaterialsContext.tsx`)와 **체크리스트**(`src/checklist/ChecklistContext.tsx`) 더미 데이터를 Context로 미리 만들어둠 — 아래 자료 탭·체크리스트 탭에서 그대로 재사용 예정
- [ ] 자료 탭 + 자료 추가 모달
- [ ] 체크리스트 탭 + 체크리스트 추가 모달(날짜/시간 피커)
- [ ] 회의록 리스트 + 회의록 작성/수정 풀페이지(서식 툴바 포함)
- [ ] 설정 탭
- [ ] 마이페이지
- [ ] 초대링크 처리 페이지 + 만료 안내
- [ ] (보류) 랜딩페이지 — 기획 완료되면 순서 재편입

### Phase 4 — 마무리 (예정)
- [ ] 반응형(모바일) 전체 점검
- [ ] 빈 상태/로딩/에러 화면 보완

### Phase 5 — 백엔드 연동 (프론트 완료 후 시작, 예정)
- [ ] Supabase 설정 (Auth, DB 스키마, RLS, Storage)
- [ ] 인증 연동 (이 시점에 비밀번호 재설정/이메일 인증 여부 결정)
- [ ] 화면별 실데이터 연동
- [ ] 실시간 기능(위임 토스트 등) 연동
- [ ] 통합 QA

## 7. 실행 방법

```
cd teamkit/app
npm install   # 최초 1회 또는 새 컴퓨터에서
npm run dev   # 개발 서버 실행 (기본 http://localhost:5173)
```

## 8. 개발 중 화면 확인 방법 (중요)

이제 로그인/회원가입이 실제로(로컬 mock 인증으로) 동작함. `/projects`, `/my-page`, `/projects/:id/*`는 로그인해야 접근 가능.

- **테스트 계정**: `test@teamkit.com` / `test1234` — 로그인 화면의 "테스트 계정으로 로그인" 버튼 누르면 원클릭 로그인됨
- 계정 데이터는 브라우저 localStorage에만 저장되므로 다른 컴퓨터/브라우저에서는 테스트 계정으로 로그인하거나 회원가입부터 새로 하면 됨 (서버에 저장되는 게 아님, Phase 5에서 Supabase로 교체 예정)
- 로그아웃은 화면 하단 DEV 이동 바 우측의 "로그아웃" 버튼으로 가능 (정식 로그아웃 버튼은 Step 8~9에서 설정/마이페이지에 넣을 예정)
- 아직 안 만든 화면(마이대시보드 이후)은 로그인 후 화면 하단 **DEV 이동 바**의 링크로 이동
- 새로 만든 UI 컴포넌트 모음: `/dev/ui-kit`
- `DevNav`(`src/dev/DevNav.tsx`)와 `/dev/ui-kit`은 실제 페이지들이 다 완성되면(Phase 3~4) 제거할 임시 개발용 화면

## 9. 업데이트 내역

- 2026-09-23: 기획/IA/와이어프레임/스타일가이드 이해 완료, 라우트 구조 확정, Phase 1(프로젝트 뼈대) 완료.
- 2026-09-23: Phase 2(공통 디자인 시스템 컴포넌트) 완료. 인증/네비게이션이 아직 없어 화면 확인이 어렵다는 피드백으로 임시 DEV 이동 바(`DevNav`)와 컴포넌트 데모 페이지(`/dev/ui-kit`) 추가.
- 2026-09-23: 피드백 반영 2차 수정 — DatePicker 오늘/선택 날짜 강조 색상 반전(오늘=연하게, 선택=브랜드퍼플 진하게), "시간 추가" 토글로 시간 미지정 가능하게 변경, 시간 선택 UI를 iOS 스타일 휠 피커(CSS scroll-snap, 외부 라이브러리 없이 자체 구현)로 교체, 솔리드 버튼 hover/active를 더 진하게, 전역 CSS로 모든 버튼에 `cursor: pointer` 적용, 저장 버튼 필수값 미입력 시 soft 톤/입력 완료 시 solid 톤으로 전환되는 패턴을 Button 컴포넌트 레벨로 구현(회의록 저장 버튼에도 동일 규칙 적용 예정, Phase 3에서 실제 폼 연결).
- 2026-09-23: 피드백 반영 3차 수정 — 토글 스위치 슬라이드 방향 버그 수정(기준 위치 명시 안 해서 반대로 밀리던 문제), "시간 추가" 줄 좌우 정렬(justify-between) + 선택된 시간 텍스트를 토글 왼쪽에 추가 표기, 휠 피커 하이라이트 배경이 숫자를 가리던 z-index 문제 수정, "+" 아이콘 크기 확대.
- 2026-09-23: Phase 3 순서 재조정(랜딩페이지 보류, 로그인/회원가입 먼저) 후 **로그인/회원가입 + 임시 mock 인증** 완료. `src/auth/AuthContext.tsx`(localStorage 기반, 테스트 계정 시드), `RequireAuth`/`RedirectIfAuthed` 라우트 가드 추가, `/`는 임시로 `/login`으로 리다이렉트.
- 2026-09-23: **마이대시보드 `/projects`** 완료. 작업 중 `Dropdown` 컴포넌트가 trigger를 또 `<button>`으로 감싸서 button-in-button이 되던 버그 발견 및 수정(Phase 2부터 있던 잠재 버그).
- 2026-09-23: 케밥 메뉴 수정 — 고정 항목 제거(핀 아이콘과 중복), "수정" 메뉴 추가(생성 모달 재사용, 기존 값 프리필), "초대 링크 복사" 메뉴 추가(첫 클릭 시 토큰 발급 후 클립보드 복사, Toast로 알림). 삭제 확인은 아직 브라우저 기본 `window.confirm`을 임시로 쓰고 있음 — 예쁜 확인 다이얼로그로 교체 예정(다음 항목 참고).
- 2026-09-23: 마이대시보드 UI 다듬기 — 정렬 드롭다운을 섹션 타이틀 바로 옆으로 이동(기존엔 반대쪽 끝에 있었음), 섹션 타이틀 24px로 확대, 타이틀 크기 위계(30/24/20px) 확정, 브라우저 기본 `window.confirm` 삭제 확인을 자체 제작 `ConfirmDialog`로 교체(`Button danger` variant 추가).
- 2026-09-23: 케밥 메뉴 "삭제" 항목 hover 시 텍스트가 보라색으로 바뀌던 버그 수정 — `DropdownItem` 기본 hover 색상(보라)과 개별 danger 텍스트 색상이 같은 우선순위로 충돌한 게 원인. `DropdownItem`에 `tone="danger"` prop을 추가해 아예 다른 색상 클래스 세트를 쓰도록 구조적으로 해결(앞으로 다른 삭제 메뉴에도 재사용 가능).
- 2026-09-23: Step 3-①(프로젝트 데이터 공유화) 완료 — 마이대시보드에만 있던 더미 프로젝트 데이터·고정/생성/수정/삭제/초대링크 로직을 `src/projects/ProjectsContext.tsx`(`ProjectsProvider`/`useProjects`)로 이동. `App.tsx`에 Provider 연결, `ProjectsPage.tsx`는 이제 이 Context를 사용.
- 2026-09-23: Step 3-②(상단 프로젝트 전환 드롭다운) 완료 — `ProjectLayout` 사이드바 상단의 "프로젝트 #id" placeholder를 실제 프로젝트 이름 + 드롭다운으로 교체, `useProjects()`로 내가 속한 다른 프로젝트 목록을 보여주고 클릭하면 해당 프로젝트 대시보드로 이동.
- 2026-09-23: Step 3-③(사이드바 하단 사용자 이름) 완료 — `ProjectLayout` 사이드바 하단에 `useAuth()`로 로그인한 사용자 아바타+이름 표시(와이어프레임 반영).
- 2026-09-23: Step 3-④(잘못된 프로젝트 ID 처리) 완료 — `ProjectLayout`에서 URL의 `:projectId`가 `ProjectsContext`에 없으면 `/projects`로 즉시 리다이렉트. **Step 3(프로젝트 상세 공통 레이아웃) 전체 완료.**
- 2026-09-23: Step 4(대시보드 탭) 완료 — `MaterialsContext`/`ChecklistContext` 신규 생성(프로젝트 1번에 더미 자료·체크리스트 시드), `ProjectDashboardPage`에서 체크리스트 요약(공통/개인, 완료 토글 동작)과 고정된 자료 목록 렌더링.
